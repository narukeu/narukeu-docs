{
  "name": "narukeu-blog",
  "scripts": {
    "dev": "vitepress dev",
    "build": "npm run clean && vitepress build",
    "preview": "vitepress preview",
    "format": "prettier --ignore-path .gitignore --write .",
    "clean": "rimraf .vitepress/dist",
    "clean:all": "rimraf .vitepress/dist .vitepress/cache"
  },
  "license": "CC0-1.0",
  "packageManager": "pnpm@10.20.0",
  "devDependencies": {
    "prettier": "^3.6.2",
    "rimraf": "^6.1.0",
    "vitepress": "2.0.0-alpha.12"
  },
  "dependencies": {
    "mermaid": "^11.12.1",
    "vue": "^3.5.24"
  }
}
